version: "2"

sql:
  - engine: "postgresql"
    schema:
      - "migrations/*.up.sql"
    queries: "sql/queries"
    gen:
      go:
        package: "dbgen"
        out: "internal/dbgen"
        sql_package: "pgx/v5"